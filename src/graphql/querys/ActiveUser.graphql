query ActiveUser {
  activeUser {
    id
    email
    enabled
    status
    isTempPassword
    updatedAt
    name
    role {
      allowApp
      permissions {
        BACKOFFICE {
          id
          name
          code
        }
      }
    }
  }
}
